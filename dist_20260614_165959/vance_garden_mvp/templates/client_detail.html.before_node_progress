<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<title>Client Detail</title>
<style>
body{background:#111;color:#fff;font-family:monospace;padding:30px;}
h1,h2{letter-spacing:2px;}
a{color:#67e8f9;}
.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin:22px 0;}
.card{border:1px solid #555;border-radius:8px;padding:18px;margin-bottom:18px;}
.metric{color:#22c55e;font-weight:bold;font-size:24px;}
.small{color:#aaa;font-size:12px;text-transform:uppercase;letter-spacing:1px;}
.item{border-top:1px dashed #555;padding:12px 0;}
.warning{color:#fbbf24;font-weight:bold;}
</style>
</head>
<body>

<p><a href="/clients">← Back to Clients</a></p>

<h1>{{ client.brokerage_name }}</h1>
<p>Client Intelligence Dossier</p>

<div class="grid">
  <div class="card">
    <div class="small">Transaction Volume</div>
    <div class="metric">${{ "{:,.0f}".format(client.transaction_volume) }}</div>
  </div>

  <div class="card">
    <div class="small">Gross GCI</div>
    <div class="metric">${{ "{:,.0f}".format(client.gci) }}</div>
  </div>

  <div class="card">
    <div class="small">Advisor Count</div>
    <div class="metric">{{ client.agent_count }}</div>
  </div>
</div>

<div class="grid">
  <div class="card">
    <div class="small">Overall IPE</div>
    <div class="metric">{{ analysis.overall_ipe }}/100</div>
  </div>

  <div class="card">
    <div class="small">Target IPE</div>
    <div class="metric">{{ analysis.target_ipe }}/100</div>
  </div>

  <div class="card">
    <div class="small">Maturity Level</div>
    <div class="metric">{{ analysis.maturity_level }}</div>
  </div>
</div>

<div class="card">
  <h2>VANCE Recommendation</h2>
  <p class="warning">{{ analysis.recommendation }}</p>
  <p>Weakest Pillar: {{ analysis.weakest_pillar }} — {{ analysis.weakest_score }}/100</p>
  <p>IPE Gap: {{ analysis.ipe_gap }} points</p>
</div>

<div class="card">
  <h2>IPE Framework</h2>
  {% for label, score in analysis.ipe_scores.items() %}
    <div>{{ label }}: <span class="metric" style="font-size:16px;">{{ score }}</span></div>
  {% endfor %}
</div>

<div class="card">
  <h2>90-Day Client Action Plan</h2>

  <div class="grid">
    <div class="card">
      <div class="small">01 // Days 0-30</div>
      <ul>
      {% for item in analysis.action_plan.days_0_30 %}
        <li>{{ item }}</li>
      {% endfor %}
      </ul>
    </div>

    <div class="card">
      <div class="small">02 // Days 31-60</div>
      <ul>
      {% for item in analysis.action_plan.days_31_60 %}
        <li>{{ item }}</li>
      {% endfor %}
      </ul>
    </div>

    <div class="card">
      <div class="small">03 // Days 61-90</div>
      <ul>
      {% for item in analysis.action_plan.days_61_90 %}
        <li>{{ item }}</li>
      {% endfor %}
      </ul>
    </div>
  </div>
</div>

<div class="card">
  <h2>Pain Point</h2>
  <p>{{ client.pain_point }}</p>

  <h2>Strategic Goal</h2>
  <p>{{ client.business_goal }}</p>
</div>

<div class="card">
  <h2>Assessment History</h2>
  <p>Total assessments: {{ assessment_count }}</p>

  {% for h in history %}
  <div class="item">
    <b>{{ h.timestamp }}</b><br>
    Volume: ${{ "{:,.0f}".format(h.transaction_volume) }} |
    GCI: ${{ "{:,.0f}".format(h.gci) }} |
    Advisors: {{ h.agent_count }}
  </div>
  {% endfor %}
</div>

</body>
</html>
